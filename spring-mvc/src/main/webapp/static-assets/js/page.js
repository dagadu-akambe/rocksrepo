/**
 * 
 */
//var DashboardElement = new DashboardElement || {};

$(document).ready(function() {
	//instantiate the main element to kick off the Beginning...
	var dashboardElement = new DashboardElement({
		$target : $('.page-content')
	});
});
